# ============================================================================
# MyBlog — Environment Variables
# Copy to .env.local and fill in your values.
# Variables prefixed with NEXT_PUBLIC_ are exposed to the browser.
# ============================================================================

# ── DATABASE (required) ──────────────────────────────────
# PostgreSQL connection string — use pooled URL for Vercel/serverless.
DATABASE_URL="postgresql://myblog:myblog_dev_password@localhost:5432/myblog"
DATABASE_URL_UNPOOLED="postgresql://myblog:myblog_dev_password@localhost:5432/myblog"

# ── CACHE (Upstash Redis — optional) ─────────────────────
# If not set, caching/rate-limiting use in-memory no-op fallback.
# UPSTASH_REDIS_REST_URL="https://your-redis.upstash.io"
# UPSTASH_REDIS_REST_TOKEN="your-token"

# ── AUTH (required) ──────────────────────────────────────
# Generate: openssl rand -base64 32
AUTH_SECRET="generate-with-openssl-rand-base64-32"
NEXTAUTH_URL="http://localhost:3000"

# ── VPS / Docker (required for non-Vercel deploys) ───────
# Prevents Server Action version-skew during rolling deploys.
# Generate: openssl rand -hex 32
# NEXT_SERVER_ACTIONS_ENCRYPTION_KEY=""

# ── AI (optional) ────────────────────────────────────────
# OPENAI_API_KEY=""

# ── CAPTCHA (optional — supports multiple providers) ────
# CLOUDFLARE_TURNSTILE_SECRET=""
# NEXT_PUBLIC_TURNSTILE_SITE_KEY=""
# NEXT_PUBLIC_RECAPTCHA_V2_SITE_KEY=""
# NEXT_PUBLIC_RECAPTCHA_V3_SITE_KEY=""
# NEXT_PUBLIC_HCAPTCHA_SITE_KEY=""

# ── PUBLIC SITE URL (optional) ──────────────────────────
# Used for OG images, canonical URLs, distribution links.
# NEXT_PUBLIC_SITE_URL="https://yourdomain.com"

# ── MEDIA / S3 STORAGE (optional) ──────────────────────
# Falls back to local filesystem (public/uploads/) if not set.
# S3_BUCKET=""
# S3_REGION=""
# S3_ACCESS_KEY_ID=""
# S3_SECRET_ACCESS_KEY=""
# S3_ENDPOINT=""
# S3_PUBLIC_URL=""

